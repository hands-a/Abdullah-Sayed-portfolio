/**
 * Spotlight
 * Radial gradient that follows the mouse cursor.
 * The CSS variable --mx/--my is set by useMousePosition hook.
 */
export default function Spotlight() {
  return <div className="spotlight" aria-hidden="true" />;
}
